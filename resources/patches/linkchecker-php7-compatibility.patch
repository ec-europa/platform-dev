Index: linkchecker.module
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- linkchecker.module	(revision a124abb6b302f2303893e4e1f083c4a05e3cb60b)
+++ linkchecker.module	(revision )
@@ -1548,7 +1548,8 @@
   foreach (field_info_instances($entity_type, $bundle_name) as $field_name => $instance) {
     $field = field_info_field($field_name);
     // #1923328: field_name array may be missing in $entity.
-    $entity_field = isset($entity->$field['field_name']) ? $entity->$field['field_name'] : array();
+    $field_name = $field['field_name'];
+    $entity_field = isset($entity->$field_name) ? $entity->$field_name : array();
 
     switch ($field['type']) {
       // Core fields.
