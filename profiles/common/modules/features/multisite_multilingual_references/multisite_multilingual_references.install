<?php

/**
 * @file
 * Install, update and uninstall functions for the Multilingual Entity References module.
 */

include_once 'multisite_multilingual_references.install.inc';

/**
 * Implements hook_install().
 */
function multisite_multilingual_references_install() {
  //Nothing yet
}

/**
 * Implements hook_uninstall().
 */
function multisite_multilingual_references_uninstall() {
  //Nothing yet
}

/**
 * Implements hook_enable().
 */
function multisite_multilingual_references_enable() {
  //Activation message
  drupal_set_message(t('Multisite entity references %v feature is now active on your site.', array('%v' => _multisite_multilingual_references_get_version())));
}

/**
 * Implements hook_disable().
 */
function multisite_multilingual_references_disable() {
  //Activation message
  drupal_set_message(t('Multisite entity references %v feature is now disabled on your site.', array('%v' => _multisite_multilingual_references_get_version())));
}


