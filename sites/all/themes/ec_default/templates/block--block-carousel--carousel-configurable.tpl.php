<?php/** * @file * Default theme implementation to display a block. * * Available variables: * - $block->subject: Block title. * - $content: Block content. * - $block->module: Module that generated the block. * - $block->delta: An ID for the block, unique within each module. * - $block->region: The block region embedding the current block. * - $classes: String of classes that can be used to style contextually through *   CSS. It can be manipulated through the variable $classes_array from *   preprocess functions. The default values can be one or more of the *   following: *   - block: The current template type, i.e., "theming hook". *   - block-[module]: The module generating the block. For example, the user *     module is responsible for handling the default user navigation block. In *     that case the class would be 'block-user'. * - $title_prefix (array): An array containing additional output populated by *   modules, intended to be displayed in front of the main title tag that *   appears in the template. * - $title_suffix (array): An array containing additional output populated by *   modules, intended to be displayed after the main title tag that appears in *   the template. * * Helper variables: * - $classes_array: Array of html class attribute values. It is flattened *   into a string within the variable $classes. * - $block_zebra: Outputs 'odd' and 'even' dependent on each block region. * - $zebra: Same output as $block_zebra but independent of any block region. * - $block_id: Counter dependent on each block region. * - $id: Same output as $block_id but independent of any block region. * - $is_front: Flags true when presented in the front page. * - $logged_in: Flags true when the current user is a logged-in member. * - $is_admin: Flags true when the current user is an administrator. * - $block_html_id: A valid HTML ID and guaranteed unique. * * @see template_preprocess() * @see template_preprocess_block() * @see template_process() * * @ingroup themeable */$first = TRUE;  global $base_url;?>  <?php print render($title_prefix); ?><?php if ($block->subject): ?><li class="nav-header"><?php print $block->subject ?></li><?php endif;?>  <?php print render($title_suffix); ?><li id="<?php print $block_html_id; ?>" class="<?php print $classes; ?> carousel slide">  <div class="carousel-inner">  <?php   foreach ($content as $id => $item):       $output = '<div class="item ' . ($first ? "active" : "") . '">';        switch ($item['type']) {      case 'image':        $thumb = image_style_url('landscape', $item['uri']);        $output .= '<a href="#">';          $output .= '<img src="' . $thumb . '" alt="' . $item['filename'] . '" />';          $output .= '<p class="carousel-caption">' . $item['field_picture_description']['und'][0]['safe_value'] . '</p>';        $output .= '</a>';      break;            case 'video':        $thumb = image_style_url('landscape', file_load($item['thumbnail'])->uri);        $output .= '<a href="#">';          $output .= '<img src="' . $thumb . '" alt="' . $item['filename'] . '" />';          $output .= '<p class="carousel-caption">' . $item['filename'] . '</p>';        $output .= '</a>';      break;            default:      break;    }    $output .= '</div>';         print $output;    $first = FALSE;   endforeach; ?>  </div>  <a class="carousel-control left" href="#<?php print $block_html_id; ?>" data-slide="prev">&lsaquo;</a>  <a class="carousel-control right" href="#<?php print $block_html_id; ?>" data-slide="next">&rsaquo;</a>    </li><?php /*sites/default/files/styles/square_thumbnail/public/user_male_portrait.png[filename] => user_male_portrait.png*/?>