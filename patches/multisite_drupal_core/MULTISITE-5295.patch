--- sites/all/modules/contributed/media_vimeo/includes/MediaVimeoStreamWrapper.inc
+++ sites/all/modules/contributed/media_vimeo/includes/MediaVimeoStreamWrapper.inc
@@ -29,7 +29,7 @@ class MediaVimeoStreamWrapper extends MediaReadOnlyStreamWrapper {
     $oembed_url = url('http://vimeo.com/api/oembed.json', array('query' => array('url' => $external_url)));
     $response = drupal_http_request($oembed_url);
 
-    if(strlen(trim($response->error)) == 0) {
+    if (!isset($response->error)) {
       $data = drupal_json_decode($response->data);
       return $data['thumbnail_url'];
     }
